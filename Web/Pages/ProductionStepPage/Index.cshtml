@page
@model Web.Pages.ProductionStepPage.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductionStep[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductionStep[0].Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ProductionStep) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.StepId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.StepId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.StepId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
